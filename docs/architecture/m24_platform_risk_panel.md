# M24 平台层风险提示面板增强

## 目标

在 `M23` 完成脚本层摘要条带增强之后，继续统一平台层的阅读节奏，把当前单独一排的 `avoid_patterns` 标签升级成更明确的风险提示面板。

本阶段目标：
- 将平台层风险项从普通标签提升为更清晰的风险提示卡。
- 保留现有 `avoid_patterns` 真值字段，不新增后端接口。
- 让平台层形成“平台摘要卡 + 风险提示面板”的完整阅读结构。

## 原材料来源

本阶段直接参考：
- `/home/admin2/smy/upstream-materials/react-bits-main/src/ts-default/Components/ProfileCard/ProfileCard.tsx`

## 适配原则

- 保留上游 `ProfileCard` 的核心语义：通过更清晰的信息分区提升关键注意事项的辨识度。
- 不引入额外交互或依赖，只借“信息分层卡片”的组织方式。
- 风险提示面板只重组现有 `avoid_patterns` 数组，不创造新的业务真值。
- 平台摘要卡主体保持不变，风险项作为并列信息区补强，而不是替代摘要层。

## 变更范围

### 平台层
- 新增独立的风险提示面板组件。
- 将 `avoid_patterns` 逐项展示为带标题语义的风险卡。
- 保留原有平台摘要卡字段展示。

### 结果页样式
- 新增平台层风险提示面板组件与样式文件。
- 不改动多模态层、机器层当前结构。

## 当前阶段明确不做

- 不改动后端 schema 与接口。
- 不新增风险评分、优先级或分类字段。
- 不引入新的第三方依赖。

## 验证要求

- 变更后执行 `npm run build`。
- 同步更新 `docs/changelog.md`。
- 以同一批次 Git 提交收口。
